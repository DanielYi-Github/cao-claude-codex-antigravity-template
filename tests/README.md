# Tests

Put this project's automated tests here. `scripts/verify-local.sh` runs
`python3 -m unittest discover -s tests -p 'test_*.py'`, so any `test_*.py`
file with `unittest.TestCase` classes is picked up automatically — including
non-Python code exercised through a Python harness. Delete this file once
real tests exist.
